---
description: Distill any intent into a SPEC kernel — the canonical machine contract for downstream work
---

Read the file `.agents/skills/bmad-spec/SKILL.md` and follow every instruction in it precisely.

Then address the following request:

$ARGUMENTS
