// Vercel serverless entry point.
// The full implementation is pre-bundled into _bundle.js by the build command.
// eslint-disable-next-line @typescript-eslint/no-require-imports
export default require('./_bundle').default;
