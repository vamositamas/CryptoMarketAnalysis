# formula-parser

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build formula-parser` to build the library.

## Running unit tests

Run `nx test formula-parser` to execute the unit tests via [Jest](https://jestjs.io).
