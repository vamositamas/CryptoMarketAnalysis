# data-sources

Bitcoin market data source clients for CryptoMarketAnalysis.

## Building

Run `nx build data-sources` to build the library.

## Running unit tests

Run `nx test data-sources` to execute the unit tests via Jest.
